<%= form_for [@product, @comment], remote: true do |f| %>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <%= f.hidden_field :product_id %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
